import pygame\n\nclass UIRenderer:\n    def __init__(self, width, height):\n        pygame.init()\n        self.screen = pygame.display.set_mode((width, height))\n        self.clock = pygame.time.Clock()\n        self.font = pygame.font.Font(None, 36)\n        self.running = True\n\n    def draw_left_panel(self, time, round_number, speed):\n        panel_width = 200\n        pygame.draw.rect(self.screen, (50, 50, 50), (0, 0, panel_width, self.screen.get_height()))\n        time_text = self.font.render(f'Time: {time}', True, (255, 255, 255))\n        round_text = self.font.render(f'Round: {round_number}', True, (255, 255, 255))\n        speed_text = self.font.render(f'Speed: {speed}', True, (255, 255, 255))\n        self.screen.blit(time_text, (10, 10))\n        self.screen.blit(round_text, (10, 50))\n        self.screen.blit(speed_text, (10, 90))\n\n    def draw_center_map(self, map_surface):\n        map_x = 200\n        map_width = self.screen.get_width() - 200\n        self.screen.blit(map_surface, (map_x, 0))\n\n    def draw_right_panel(self, kill_feed, ai_coach, player_roster):\n        panel_width = 200\n        pygame.draw.rect(self.screen, (50, 50, 50), (self.screen.get_width() - panel_width, 0, panel_width, self.screen.get_height()))\n        y_offset = 10\n        for kill in kill_feed:\n            kill_text = self.font.render(kill, True, (255, 255, 255))\n            self.screen.blit(kill_text, (self.screen.get_width() - panel_width + 10, y_offset))\n            y_offset += 30\n\n        ai_text = self.font.render(f'AI Coach: {ai_coach}', True, (255, 255, 255))\n        self.screen.blit(ai_text, (self.screen.get_width() - panel_width + 10, y_offset))\n        y_offset += 30\n\n        for player in player_roster:\n            roster_text = self.font.render(player, True, (255, 255, 255))\n            self.screen.blit(roster_text, (self.screen.get_width() - panel_width + 10, y_offset))\n            y_offset += 30\n\n    def run(self):\n        while self.running:\n            for event in pygame.event.get():\n                if event.type == pygame.QUIT:\n                    self.running = False\n\n            self.screen.fill((0, 0, 0))  # Clear the screen\n            self.draw_left_panel('00:00', 1, 0)  # Example values\n            self.draw_center_map(pygame.Surface((self.screen.get_width() - 200, self.screen.get_height())))  # Example map surface\n            self.draw_right_panel(['Player1 killed Player2'], 'Coach AI', ['Player1: 1000/10', 'Player2: 800/5'])  # Example values\n            pygame.display.flip()  # Update the display\n            self.clock.tick(60)  # Limit to 60 frames per second\n\n    def quit(self):\n        pygame.quit()\n\nif __name__ == '__main__':\n    renderer = UIRenderer(800, 600)\n    renderer.run()\n    renderer.quit()